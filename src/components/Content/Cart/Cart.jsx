import React from "react";
import cls from "../GoodsList/GoodsList.module.scss";

const Cart = () => {
    return (
        <h2 className={cls["main__title"]}>
            Cart Items
        </h2>
    );
};

export default Cart;